"""Reserved, and deliberately empty.

The plan this package was created for -- a Lua API database as its own
subpackage -- landed as :mod:`dsotools.scriptdoc` instead, because the database
turned out to be one module and two generated JSON files rather than a layer.

Kept rather than deleted only because ``dsotools.api`` appears in the published
layout; it exports nothing and nothing imports it.
"""
